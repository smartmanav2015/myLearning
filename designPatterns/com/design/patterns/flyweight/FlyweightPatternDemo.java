package com.design.patterns.flyweight;



/**
 * Flyweight pattern is primarily used to reduce the number of objects 
 * created, to decrease memory footprint and increase performance
 * 
 * @author asax22
 *
 */
public class FlyweightPatternDemo {
	
	   private static final String bankId[] = { "Red", "Green", "Blue", "White", "Black" };
	   public static void main(String[] args) {

	     /* for(int i=0; i < 20; ++i) {
	         Circle circle = (Circle)BankDetailsFactory.getCircle(getRandomColor());
	         circle.setX(getRandomX());
	         circle.setY(getRandomY());
	         circle.setRadius(100);
	         circle.draw();
	      }*/
	   }
	  
	}